The commands used for monitoring real-time logs of an app are:
> adb logcat | findstr com.package.name : It diplays the log of the app by the specified package name.
> adb logcat | findstr com.package.name > applog.txt : It stores the log of the app as a text file
